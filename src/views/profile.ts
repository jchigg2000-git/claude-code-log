// The Profile tab was originally a hardcoded essay about this repo's
// owner, written from their own corpus. It named private projects and
// personal details that have no place in a public history, so these
// historical revisions were replaced when the repo was opened up.
//
// The tab was rebuilt to compute itself live from GET /api/metrics, with
// nothing about the operator hardcoded. See src/views/profile.ts at HEAD.
export {};
